package com.tibco.devtools.buildscripts.tasks.yguard;

import com.tibco.devtools.buildscripts.BuildTask;

public class Package
    extends BuildTask
{

    public Package(String type, boolean isText, boolean isEmpty)
    {
        super(type, isText, isEmpty);
        // TODO Auto-generated constructor stub
    }

    // name, map, both required
}
