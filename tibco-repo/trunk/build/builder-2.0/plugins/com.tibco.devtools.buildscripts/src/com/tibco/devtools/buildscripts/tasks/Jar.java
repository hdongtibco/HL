package com.tibco.devtools.buildscripts.tasks;

public class Jar
    extends Zip
{

    public Jar(String basedir)
    {
        super(JAR, basedir);
    }

    // manifest filesetmanifest whenmanifestonly manifestencoding

    // metainf manifest indexjars service
}
